[package]
name = "kernel"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "The observation and collection kernel extracted from muon"

[features]
default = ["std"]
alloc = []
std = ["alloc"]

[dependencies]
kernel-macros = { path = "macros" }
